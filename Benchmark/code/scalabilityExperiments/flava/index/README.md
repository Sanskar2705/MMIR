# Index Directory

FAISS index files for FLAVA scalability experiments will be saved here.

- `flava_image_flat.index` — Flat (exact) FAISS index
- `flava_image_hnsw.index` — HNSW approximate FAISS index
- `image_names.json` — Mapping of FAISS vector positions to image names

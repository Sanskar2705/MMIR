# Index Directory

FAISS index files for UniIR scalability experiments will be saved here.

- `uniir_image_flat.index` — Flat (exact) FAISS index
- `uniir_image_hnsw.index` — HNSW approximate FAISS index
- `image_names.json` — Mapping of FAISS vector positions to image names

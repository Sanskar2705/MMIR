# Index Directory

FAISS index files for CLIP scalability experiments will be saved here.

- `clip_image_flat.index` — Flat (exact) FAISS index
- `clip_image_hnsw.index` — HNSW approximate FAISS index
- `image_names.json` — Mapping of FAISS vector positions to image names
